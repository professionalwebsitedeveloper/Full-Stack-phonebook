version: 1
services:
  - type: web
    name: phonebook-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      NODE_ENV: production
    autoDeploy: true